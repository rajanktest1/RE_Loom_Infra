apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: realestate
spec:
  selector:
    app: inventory-service
  ports:
    - port: 4002
      targetPort: 4002
      protocol: TCP
