apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: realestate
spec:
  selector:
    app: document-service
  ports:
    - port: 4006
      targetPort: 4006
      protocol: TCP
