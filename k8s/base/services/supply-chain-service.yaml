apiVersion: v1
kind: Service
metadata:
  name: supply-chain-service
  namespace: realestate
spec:
  selector:
    app: supply-chain-service
  ports:
    - port: 4003
      targetPort: 4003
      protocol: TCP
