apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: realestate
spec:
  selector:
    app: notification-service
  ports:
    - port: 4005
      targetPort: 4005
      protocol: TCP
